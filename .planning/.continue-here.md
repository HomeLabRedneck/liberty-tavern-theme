---
context: default
phase: post-03-layout-fixes
task: 10
total_tasks: 11
status: in_progress
last_updated: 2026-04-29T21:21:57Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Wrong layout layer — CSS rules on `.container` / `.discovery-list-container` do NOT fix the banner left gap. Gap lives at `#main-outlet-wrapper` flex gap level. Target that first.
- [ ] CONSTRAINT: CSS variable resolution — `var(--tavern-brass)` and other `var(--tavern-*)` custom properties defined in `:root` may not resolve in Discourse theme preview. Use hex literals for all critical colors.
- [ ] CONSTRAINT: Double padding-top — Do NOT add `padding-top` to `#main-outlet-wrapper`. Discourse auto-sets `body { padding-top: var(--header-offset) }` via JS. Manual addition doubles the gap.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Target wrong layout layer | Spent 3 commits fixing `.container` padding when gap came from `#main-outlet-wrapper` flex gap | blocking | Always identify which DOM layer the gap comes from before writing CSS — sidebar touching = wrapper OK, banner not = look inside #main-outlet |
| CSS variable blind spot | `var(--tavern-brass)` used in header border; variable didn't resolve → wrong color shown | advisory | Use hex for all header/banner colors; keep `:root` vars only for places confirmed to resolve |
| Manual header offset | Adding `#main-outlet-wrapper { padding-top: 64px }` on top of Discourse's auto `--header-offset` | blocking | Never add manual header offset; Discourse JS handles it |

<current_state>
Post-Phase-3 layout polish. Branch: `phase-3-homepage-content`. Responding to user screenshots of live Discourse instance. 

Progress this session:
- Banner left gap: FIXED — `gap: 0` on `#main-outlet-wrapper`, zeroed all intermediate containers
- Trending alignment: FIXED — `margin: 0 64px` centers it with topic list, not touching sidebar
- Topic list centering: FIXED — `margin: 0 64px; width: calc(100% - 128px)` 
- Header border color: FIXED — hardcoded `#D49838`
- Banner top gap (cream between header and banner): PARTIALLY FIXED — latest commit (1f1b0ce) zeroes `#main-outlet > *` top margins. Not yet confirmed by user.
</current_state>

<completed_work>

- Task 1: `.remember/logs` dir created at project root
- Task 2: Banner left gap — zeroed `.container`, `.discovery-list-container` (commit 536371a)
- Task 3: Flex gap fix — `#main-outlet-wrapper { gap: 0 }` (commit d05d719)
- Task 4: Trending centered — `margin: 0 64px` (commit 58e91a0)
- Task 5: Trending padding restored — `padding: 32px 24px 24px` (commit c3c8c47)
- Task 6: Removed double `padding-top: 64px` from `#main-outlet-wrapper` (commit 5d5d014)
- Task 7: Header border `#D49838` hex + `.list-controls` all-zero (commit 69df397)
- Task 8: `#main-outlet padding: 0` + `[data-outlet]` margin: 0 (commit 21ebd54)
- Task 9: `#main-outlet > * { margin-top: 0; padding-top: 0 }` (commit 1f1b0ce)
</completed_work>

<remaining_work>

- Task 10: **Confirm** banner top gap closed — user must update theme + report result
  - If gap persists: inspect live DOM for any remaining top margin/padding above `.tavern-banner`
  - Fallback: `body.navigation-topics, body.navigation-categories { .tavern-banner { margin-top: -Npx } }`
- Task 11: Plan Phase 4 via `/gsd-plan-phase 4` (Right Column — Badges + House Rules panels)
</remaining_work>

<decisions_made>

- `margin: 0 64px` on `.tavern-trending` (not padding) — centers box, doesn't affect full-width
- `margin + width: calc(100% - 128px)` on `.topic-list` — tables need margin not padding for visual centering
- `border-bottom: 2px solid #D49838` hardcoded — CSS vars unreliable in Discourse theme context
- Removed `#main-outlet-wrapper { padding-top: 64px }` — Discourse auto-sets `--header-offset`
- `gap: 0` on `#main-outlet-wrapper` for homepage — sidebar/content flex gap was banner left-gap root cause
</decisions_made>

<blockers>

- Banner top gap (small cream strip between header and banner): latest fix not yet user-confirmed
</blockers>

## Required Reading (in order)
1. `.planning/STATE.md` — current phase position (Phase 3 done, Phase 4 ready)
2. `.planning/ROADMAP.md` — Phase 4 requirements (COL-01..04, right column at 1160px+)
3. `discourse-theme/CLAUDE.md` — critical Discourse API rules and anti-patterns
4. `ColorSpec.md` — authoritative color reference for all elements

## Critical Anti-Patterns (do NOT repeat these)
- **Wrong layer targeting**: Banner left gap came from `#main-outlet-wrapper` flex gap, NOT `.container` padding → always trace to actual flex/grid parent
- **var() in header**: `var(--tavern-brass)` didn't resolve → use `#D49838` hex
- **Double offset**: Don't add `padding-top` to `#main-outlet-wrapper`

## Infrastructure State
- Branch: `phase-3-homepage-content` (ahead of main, all committed)
- Uncommitted: `.planning/phases/03-homepage-content/03-PATTERNS.md`, `.planning/ui-reviews/` (untracked, safe to leave)
- Remote: pushed to `origin/phase-3-homepage-content`

<context>
All fixes this session were reactive to user screenshots. The formal GSD workflow (phases 1-3) completed before this session. We're doing visual polish on the live forum. The header, banner left edge, trending centering, and topic list centering are all resolved. The only open item is the small top gap between header and banner — reduced significantly (Image #5 showed it smaller than before), latest fix zeroes Glimmer route wrapper margins. Phase 4 (right column) is the next formal work item.
</context>

<next_action>
Ask user to update theme in Discourse + hard refresh, confirm if banner top gap (cream strip below header) is gone. If confirmed, run `/gsd-plan-phase 4`.
</next_action>
