---
phase: 01-foundation-repair
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - about.json
  - javascripts/discourse/api-initializers/theme-setup.js
  - javascripts/discourse/connectors/below-site-header/tavern-banner.hbs
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03

must_haves:
  truths:
    - "The homepage banner renders exactly once, positioned above the topic list"
    - "Discourse's native WelcomeBanner does not render alongside our banner"
    - "The connector file at connectors/below-site-header/ no longer exists"
    - "theme-setup.js mounts TavernBanner via api.renderInOutlet at discovery-list-container-top"
    - "about.json has enable_welcome_banner set to false in theme_site_settings"
  artifacts:
    - path: "javascripts/discourse/api-initializers/theme-setup.js"
      provides: "api-initializer that mounts TavernBanner at discovery-list-container-top"
      exports: ["default (apiInitializer)"]
    - path: "about.json"
      provides: "theme manifest with theme_site_settings disabling WelcomeBanner"
      contains: "enable_welcome_banner"
  key_links:
    - from: "theme-setup.js"
      to: "TavernBanner component"
      via: "api.renderInOutlet"
      pattern: "renderInOutlet.*discovery-list-container-top.*TavernBanner"
    - from: "about.json"
      to: "Discourse WelcomeBanner"
      via: "theme_site_settings.enable_welcome_banner"
      pattern: "enable_welcome_banner.*false"
---

<objective>
Fix the banner duplication bug by applying both required fixes: disable Discourse's native WelcomeBanner via about.json, and move our banner mount from the broken below-site-header connector to the discovery-list-container-top outlet via a new api-initializer.

Purpose: This is the root cause of the most visible bug. The connector file renders TavernBanner outside #main-outlet-wrapper (breaking SCSS context) while simultaneously Discourse's WelcomeBanner may render at the same outlet. Both fixes are needed.

Output: theme-setup.js (new), updated about.json, deleted connector file
</objective>

<execution_context>
@/c/Users/Jake/.claude/get-shit-done/workflows/execute-plan.md
@/c/Users/Jake/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-repair/01-CONTEXT.md
@.planning/research/SUMMARY.md

<interfaces>
<!-- Key patterns the executor needs. Extracted from existing codebase. -->

From javascripts/discourse/api-initializers/honored-patrons.js (lines 1–5) — exact analog for new theme-setup.js:
```js
import { apiInitializer } from "discourse/lib/api";
import { ajax } from "discourse/lib/ajax";

export default apiInitializer("1.13.0", (api) => {
  if (!settings.honored_patrons_enabled) return;
  // api calls here
});
```

From javascripts/discourse/connectors/below-site-header/tavern-banner.hbs (full file — DELETE this):
```hbs
<TavernBanner />
```

Current about.json structure — add theme_site_settings after color_schemes closing brace:
```json
{
  "name": "Liberty Tavern",
  "about_url": "...",
  "license_url": "...",
  "component": false,
  "color_schemes": { ... }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create theme-setup.js and delete connector file</name>
  <files>
    javascripts/discourse/api-initializers/theme-setup.js (CREATE NEW)
    javascripts/discourse/connectors/below-site-header/tavern-banner.hbs (DELETE)
  </files>
  <read_first>
    - javascripts/discourse/api-initializers/honored-patrons.js (analog — exact structure to follow)
    - javascripts/discourse/connectors/below-site-header/tavern-banner.hbs (confirm contents before deleting)
    - javascripts/discourse/components/tavern-banner.js (confirm the component export name is TavernBanner)
  </read_first>
  <action>
1. CREATE `javascripts/discourse/api-initializers/theme-setup.js` with this exact content:

```js
import { apiInitializer } from "discourse/lib/api";
import TavernBanner from "../components/tavern-banner";

export default apiInitializer("1.13.0", (api) => {
  if (!settings.show_homepage_banner) return;
  api.renderInOutlet("discovery-list-container-top", TavernBanner);
});
```

Notes:
- Version "1.13.0" matches honored-patrons.js — do not change it.
- Guard uses `settings.show_homepage_banner` (the existing setting in settings.yml, not a new one).
- No ajax import needed — this file does no async work.
- Per D-03: this is a standalone file, do NOT add to honored-patrons.js.

2. DELETE `javascripts/discourse/connectors/below-site-header/tavern-banner.hbs`.
   This file contains only `<TavernBanner />`. After deletion, the banner is mounted exclusively via api.renderInOutlet above. Leaving this file active means the banner renders twice.
   If the connectors/below-site-header/ directory becomes empty after deletion, leave the empty directory — do not delete it (git tracks file deletions, not directory deletions).
  </action>
  <verify>
    <automated>grep -r "renderInOutlet" /c/Users/Jake/Projects/LibertyTavernTheme/discourse-theme/javascripts/discourse/api-initializers/theme-setup.js</automated>
  </verify>
  <acceptance_criteria>
    - `theme-setup.js` exists at `javascripts/discourse/api-initializers/theme-setup.js`
    - `theme-setup.js` contains the string `renderInOutlet("discovery-list-container-top", TavernBanner)`
    - `theme-setup.js` contains the string `import TavernBanner from "../components/tavern-banner"`
    - `theme-setup.js` contains the string `if (!settings.show_homepage_banner) return`
    - `theme-setup.js` does NOT contain `ajax` (no ajax import)
    - `connectors/below-site-header/tavern-banner.hbs` does NOT exist (file deleted)
  </acceptance_criteria>
  <done>theme-setup.js created with correct content; connector file deleted from filesystem and staged in git</done>
</task>

<task type="auto">
  <name>Task 2: Add theme_site_settings to about.json</name>
  <files>
    about.json
  </files>
  <read_first>
    - about.json (read full file before editing — must preserve existing color_schemes structure exactly)
  </read_first>
  <action>
Add a `theme_site_settings` key to `about.json` after the `color_schemes` block. The file currently ends with the closing brace of color_schemes then the root `}`. Add the new block between them.

The resulting about.json must be:

```json
{
  "name": "Liberty Tavern",
  "about_url": "https://github.com/your-org/liberty-tavern-theme",
  "license_url": "https://github.com/your-org/liberty-tavern-theme/blob/main/LICENSE",
  "component": false,
  "color_schemes": {
    "Liberty Tavern": {
      "primary":              "#2a1f17",
      "secondary":            "#f5ebd9",
      "tertiary":             "#7a1f1f",
      "quaternary":           "#c8941a",
      "header_background":    "#1a120c",
      "header_primary":       "#f5ebd9",
      "highlight":            "#c8941a",
      "danger":               "#9a2a1a",
      "success":              "#3d6b3d",
      "love":                 "#7a1f1f",
      "selected":             "#e8dcc4",
      "hover":                "#ede0c7"
    },
    "Liberty Tavern Dark": {
      "primary":              "#f0e6d2",
      "secondary":            "#1a120c",
      "tertiary":             "#d4a548",
      "quaternary":           "#c8941a",
      "header_background":    "#0f0a06",
      "header_primary":       "#f0e6d2",
      "highlight":            "#d4a548",
      "danger":               "#c44a3a",
      "success":              "#5a8a5a",
      "love":                 "#c44a3a",
      "selected":             "#2a1f17",
      "hover":                "#241a12"
    }
  },
  "theme_site_settings": {
    "enable_welcome_banner": false
  }
}
```

Do not change any existing key or value. The only modification is adding the `theme_site_settings` block.
  </action>
  <verify>
    <automated>grep "enable_welcome_banner" /c/Users/Jake/Projects/LibertyTavernTheme/discourse-theme/about.json</automated>
  </verify>
  <acceptance_criteria>
    - `about.json` contains `"theme_site_settings"` key
    - `about.json` contains `"enable_welcome_banner": false`
    - `about.json` is valid JSON (no trailing commas, balanced braces)
    - `about.json` still contains `"Liberty Tavern"` color scheme with `"primary": "#2a1f17"`
    - `about.json` still contains `"Liberty Tavern Dark"` color scheme
    - `about.json` does NOT contain `"enable_welcome_banner": true`
  </acceptance_criteria>
  <done>about.json updated with theme_site_settings block; file is valid JSON with all existing color_schemes intact</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Theme JS → Discourse API | api.renderInOutlet is a trusted Discourse internal call; no user data crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | about.json | accept | Config file in version control; no runtime user input; JSON validity checked before commit |
| T-01-02 | Spoofing | theme-setup.js | accept | Pure frontend theme code; no auth or user identity involved; all API calls go to Discourse's own trusted endpoints |

This is a Discourse theme (pure frontend CSS/JS/HBS). No backend, no auth, no sensitive data storage. XSS risk is minimal — all data comes from Discourse's own API endpoints rendered through Discourse's own sanitization. No server-side security concerns apply.
</threat_model>

<verification>
After both tasks complete:

1. `git status` shows `theme-setup.js` as a new file, `connectors/below-site-header/tavern-banner.hbs` as deleted, `about.json` as modified.

2. Grep checks (run from repo root):
   - `grep "renderInOutlet" javascripts/discourse/api-initializers/theme-setup.js` → matches
   - `grep "enable_welcome_banner" about.json` → matches `"enable_welcome_banner": false`
   - `ls javascripts/discourse/connectors/below-site-header/tavern-banner.hbs` → "No such file"

3. Manual browser test (after theme upload):
   - Homepage shows banner exactly once above the topic list.
   - No duplicate banner text at the top of the page.
   - Browser DevTools: zero elements with class `welcome-banner` on the homepage.
</verification>

<success_criteria>
- `javascripts/discourse/api-initializers/theme-setup.js` exists with `renderInOutlet("discovery-list-container-top", TavernBanner)`
- `javascripts/discourse/connectors/below-site-header/tavern-banner.hbs` does not exist
- `about.json` contains `"enable_welcome_banner": false` inside `theme_site_settings`
- All three changes are staged in git
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-repair/01-01-SUMMARY.md`
</output>
