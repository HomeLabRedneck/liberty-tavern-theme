---
phase: 04-right-column
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified: []
autonomous: false
requirements: [COL-01, COL-02, COL-03, COL-04]
tags: [discourse-theme, live-verification, deploy, right-column]

estimate:
  tokens: 12000
  raw_tokens: 8000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "The rail renders beside main content on ALL FOUR discovery views — Rooms (.category-boxes) AND Trending / Latest at the Bar / Top Shelf (.topic-list) — at >=1160px. [COL-01, D-1]"
    - "Below 1160px the rail is hidden on all four views with no horizontal scroll and no broken layout. [COL-02, D-1]"
    - "Badges panel shows real top-4 most-awarded badges with tier hexagons and grant counts. [COL-03, D-3]"
    - "House Rules panel shows the four locked rules. [COL-04, D-4]"
    - "The grid-container selector and after-main-outlet mount used in 04-01 are confirmed correct against the live DOM on this Discourse version; any correction is captured as a gap. [D-1 open items]"
  artifacts:
    - "04-02-SUMMARY.md recording live pass/fail per view + the confirmed wrapper selector and mount strategy"
  key_links:
    - "after-main-outlet mount <-> confirmed to fire on .topic-list views (open item #2), not only .category-boxes"
    - "grid-container selector <-> confirmed against live DOM (open item #1)"
---

<objective>
Deploy Plan 04-01 to theme 15 and verify the right-column rail live across all four homepage discovery views and the responsive breakpoint. This is the phase's comprehensive live-verification gate — the same commit + deploy + browser-confirm discipline used to finish Phase 3. It confirms the two 04-CONTEXT open items (exact wrapper selector; after-main-outlet coverage on topic-list views) against the real DOM. Covers COL-01..COL-04 as observable live behavior.

Purpose: prove the phase goal is TRUE on the live preview before marking Phase 4 complete.
Output: a verification SUMMARY; any failure spawns gap closure (`/gsd-plan-phase 4 --gaps`).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-right-column/04-CONTEXT.md
@.planning/phases/04-right-column/04-UI-SPEC.md
@.planning/phases/04-right-column/04-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Deploy to theme 15 and verify the rail across all four discovery views + responsive</name>
  <precondition>The theme is git-linked as theme id 15 (branch phase-3-homepage-content, PREVIEW-only); the human has Admin access to Themes to pull updates. (user_setup)</precondition>
  <what-built>
    Plan 04-01 shipped the `TavernRightColumn` connector-component (`/badges.json` top-4 fetch + House Rules), the section-10 grid override + responsive reset in common.scss, and the `house_rules` / `show_right_column` settings. The rail was proven end-to-end on the Rooms view during the tracer; this checkpoint confirms it on the remaining three topic-list views and at narrow widths.
  </what-built>
  <how-to-verify>
    1. Ensure 04-01 is committed and pushed. In Admin -> Themes -> Liberty Tavern, click Check for updates -> Update to latest.
    2. At a viewport >=1160px, load each of the four discovery views with `?preview_theme_id=15`:
       - Rooms: `/categories?preview_theme_id=15`
       - Trending: `/hot?preview_theme_id=15` (or the configured top_menu Trending route)
       - Latest at the Bar: `/latest?preview_theme_id=15`
       - Top Shelf: `/top?preview_theme_id=15`
       On EACH: the right-column rail appears BESIDE the main content (not below, not overlapping); the Badges panel shows the 2x2 tier-hexagon grid with real names + "{N} EARNED"; the House Rules box shows the four numbered rules. On Rooms specifically: the dark banner + stats box still span the full content width above the rail.
    3. Confirm after-main-outlet coverage (open item #2): the rail is present on the three `.topic-list` views, not only on `.category-boxes` (Rooms). If it is MISSING on topic-list views, note that the renderInOutlet fallback is needed.
    4. Narrow the browser below 1160px on any view: the rail disappears, main content reflows to full width, and there is NO horizontal scrollbar and NO empty gap/track where the rail was.
    5. In DevTools, confirm the grid-container selector recorded in 04-01-SUMMARY is the element actually driving the two-column layout (open item #1). Note any correction.
  </how-to-verify>
  <resume-signal>Type "approved" if all four views + the responsive hide pass. Otherwise describe which view/selector failed (e.g. "rail missing on /latest", "horizontal scroll at 1100px", "banner narrowed on Rooms") so it can be routed to `/gsd-plan-phase 4 --gaps`.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
No new attack surface — this plan modifies no source; it deploys and visually verifies Plan 04-01. The 04-01 threat register (badge-name/image XSS mitigations T-04-01/T-04-02) governs the code under test.

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-06 | (verification only) | live deploy | low | accept | PREVIEW-only theme (preview_theme_id=15); no default-theme change, fully git-revertable. Reviewed under 04-01's `costly` reversibility rating. |

Supply-chain: not applicable — no installs.
</threat_model>

<verification>
Human live verification across the four discovery views and the <1160px breakpoint (checkpoint above). A Discourse theme has no automated test framework (REQUIREMENTS "Out of Scope: Automated tests"); the source-level assertions ran in Plan 04-01.
</verification>

<success_criteria>
- COL-01: rail beside main content on all four discovery views at >=1160px, banner full-width on Rooms.
- COL-02: rail hidden below 1160px on all views, no horizontal scroll, no broken layout.
- COL-03: Badges panel shows real top-4 tier-hexagon grid live.
- COL-04: House Rules panel shows the four rules live.
- Open items #1 (wrapper selector) and #2 (topic-list mount coverage) confirmed against the live DOM.
</success_criteria>

<output>
Create `.planning/phases/04-right-column/04-02-SUMMARY.md` recording per-view pass/fail, the confirmed wrapper selector + mount strategy, and any corrections routed to gap closure.
</output>
